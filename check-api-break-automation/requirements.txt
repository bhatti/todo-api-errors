# Core dependencies for API compatibility checker
langchain>=0.1.0
langchain-google-vertexai>=0.1.0
langgraph>=0.0.50
pydantic>=2.0.0

# Google Cloud dependencies
# Note: google-cloud-aiplatform>=1.40.0 required for some Vertex AI features
# If you see "GenerativeModel has no attribute 'list_models'" error, upgrade:
# pip install --upgrade google-cloud-aiplatform vertexai
google-cloud-aiplatform>=1.40.0
vertexai>=1.40.0  # Explicitly include vertexai package
google-auth>=2.25.0
google-auth-oauthlib>=1.1.0

# MCP (Model Context Protocol) - optional, uncomment if available
# mcp>=0.1.0

# Utility dependencies
pyyaml>=6.0
click>=8.1.0
rich>=13.7.0  # For better CLI output formatting
python-dotenv>=1.0.0  # For environment variable management

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Optional dependencies for enhanced features
# For parallel processing
asyncio>=3.4.3
aiofiles>=23.0.0

# For better diff visualization
deepdiff>=6.7.0
colorama>=0.4.6